# Key Art Assets

The current game and Steam web artwork remains available. It is not presented as a print-quality master.

Add the pending original as:

`CradleOfLife_KeyArt_Master.png`

Configure its path and source resolution in `press-kit-config.json` to reveal the `Download Key Art` action.
