# Developer Headshot

The approved biography is stored in `index.html` and generated into the downloadable press kit.

Add the pending civilian head-and-shoulders photograph here as:

`Developer_GarrettMickelsen.jpg`

Then set the `DEVELOPER_HEADSHOT` paths in `press-kit-config.json`. Do not add a military photograph or generated replacement.
